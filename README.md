# chain_health_tgbot
## Bot monitors chain mileage

# Settings
- Create /logs and - if you use sqlite as a storage - /data directories
- Set paths to logs and data in settings.toml
- Set sqlalchemy.url in alembic.ini
